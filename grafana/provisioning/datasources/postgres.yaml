apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    uid: efsudp8r58f7ke
    access: proxy
    url: host.docker.internal:5432
    user: postgres
    database: DT-MAIN
    secureJsonData:
      password: postgres123
    jsonData:
      database: DT-MAIN
      sslmode: disable
      postgresVersion: 1500
      timescaledb: false
    isDefault: true